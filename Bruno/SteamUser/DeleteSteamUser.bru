meta {
  name: DeleteSteamUser
  type: http
  seq: 3
}

delete {
  url: {{host}}/steam_users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
