meta {
  name: GetGameById
  type: http
  seq: 3
}

get {
  url: {{host}}/games/{{game_id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.id_game: eq {{game_id}}
}
