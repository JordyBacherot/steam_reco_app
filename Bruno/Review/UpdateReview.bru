meta {
  name: UpdateReview
  type: http
  seq: 2
}

put {
  url: {{host}}/reviews/{{reviewId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "text": "Updated review text via Bruno!"
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
