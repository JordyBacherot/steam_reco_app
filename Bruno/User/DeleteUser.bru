meta {
  name: DeleteUser
  type: http
  seq: 4
}

delete {
  url: {{host}}/users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
