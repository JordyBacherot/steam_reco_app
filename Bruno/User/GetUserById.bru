meta {
  name: GetUserById
  type: http
  seq: 2
}

get {
  url: {{host}}/users/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.id_user: eq {{userId}}
}
