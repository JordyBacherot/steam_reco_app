meta {
  name: GetUserLibrary
  type: http
  seq: 2
}

get {
  url: {{host}}/users/{{userId}}/games
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data: isArray
}
