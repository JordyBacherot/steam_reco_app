meta {
  name: RemoveGameFromLibrary
  type: http
  seq: 3
}

delete {
  url: {{host}}/users/{{userId}}/games/220
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
  res.body.success: eq true
}
