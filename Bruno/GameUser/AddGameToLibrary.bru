meta {
  name: AddGameToLibrary
  type: http
  seq: 1
}

post {
  url: {{host}}/users/1/games
  body: json
  auth: inherit
}

body:json {
  {
    "id_game": 220,
    "nb_hours": 105.5
  }
}

assert {
  res.status: in [200, 201]
  res.body.success: eq true
  res.body.message: isString
  res.body.data: isJson
}
